version: 2.1

orbs:
  profiling: nanophate/docker-profiling@1.2.3

job:
  build:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run: echo "hello"
  rspec:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run: echo "Rspec"
  deploy:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run: echo "deploy"
workflows:
  version: 2
  build_and_deploy:
    jobs:
      - build
      - profiling/network
      - rspec:
          requires:
            - build
      - deploy:
          requires:
            - rspec
  deploy:
    jobs:
      - build
      - profiling/network
      - deploy:
          requires:
            - build
